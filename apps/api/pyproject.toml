[project]
name = "prompt-studio-api"
version = "0.3.0"
description = "Local API for Prompt Studio CN"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.139.2",
  "uvicorn[standard]==0.51.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
